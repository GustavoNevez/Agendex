import ResponsiveHeader from './ResponsiveHeader';

/**
 * Exporta o novo componente de cabeçalho responsivo como padrão
 * O componente antigo está preservado como LegacyHeader.js para referência
 */
export default ResponsiveHeader;